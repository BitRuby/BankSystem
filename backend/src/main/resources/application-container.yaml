spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://mysql:3306/bank
    username: hr
    password: hr

  jpa:
    hibernate:
      ddl-auto: validate
    database: mysql

ftp:
  host: localhost
  port: 21599
  user: uibank
  passphrase: passub
  directory: 1160
  autostart: true
  timeout: 100